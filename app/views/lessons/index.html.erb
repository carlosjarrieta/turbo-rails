<p id="notice"><%= notice %></p>
<%= turbo_frame_tag 'course-lesson-list' do %>
  <h1> <%= link_to 'Crear lección', new_course_lesson_path(@course), class:"btn btn-primary", remote: true %></h1>
  <table class="table">
    <thead>
      <tr>
        <th>Course</th>
        <th>Title</th>
        <th>Content</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody id="lesson-list">
      <% @lessons.each do |lesson| %>
        <%= render lesson %>
      <% end %>
    </tbody>
  </table>
<% end %>
<br>
<%= link_to 'New Lesson', new_course_lesson_path(@course) %>
